import { z } from "zod";
import { zodResolver } from "@hookform/resolvers/zod";

const UploadImage = () => {
  return <div className="bg-slate-50 rounded p-6">Upload Image</div>;
};

export default UploadImage;
